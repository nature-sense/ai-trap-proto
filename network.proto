syntax = "proto3";

enum State {
  offline = 0;
  starting = 1;
  online = 2;
}

enum WiFiMode {
  client = 0;
  hotspot = 1;
}

message WifiStatus {
  WiFiMode mode = 1;
  string ssid = 2;
  State state = 3;
}

